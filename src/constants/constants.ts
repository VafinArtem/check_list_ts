export enum AuthorizationStatus {
  AUTH = 'AUTH',
  NO_AUTH = 'NO_AUTH',
  UNKNOWN = 'UNKNOWN',
}

export const NameSpace = {
  USER: `USER`,
  DATA: `DATA`
}

export enum ListType {
  COMPLETE = 'COMPLETE',
  PROCESS = 'PROCESS',
}

export enum ListName {
  COMPLETE = `Готово`,
  PROCESS = `В процессе`
}
